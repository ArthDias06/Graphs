//Arthur de Castro Dias - 1685302
//Pedro Tiago Biffi - 16
//Gabriel Carraro Salzedas - 16
//Guilherme Cavalcanti de Santana - 15456556

#include "graph.h"

int main(void)
{
    int option;
    int N, x, y, w;
    GRAPH *G;

	loop:

	/*
    printf("[0] myGraph\n");
    printf("[1] addEdge\n");
    printf("[2] existEdge\n");
    printf("[3] neighbors\n");
    printf("[4] removeEdge\n");
    printf("[5] printInfo\n");
    printf("[6] deleteGraph\n");
    printf("[7] maxNeighbors\n");
    printf("[8] adjacencyMatrix\n");
    printf("[9] sair\n");
    */
    scanf("%d ", &option);
    
    switch (option)
    {
        int x, y, w;
        case 0:
     	 	scanf("%d ", &N);
        	G = myGraph(N);
        	break;
        case 1: // add_edge
        	printf("vert1, vert2, peso\n");
        	scanf("%d %d %d ", &x, &y, &w);
            addEdge(G, x, y, w);
            break;
        case 2: // existEdge
			printf("vert1, vert2\n");
        	scanf("%d %d ", &x, &y);
        	if( existEdge(G, x, y) )
        		printf("Existe\n");
        	else
        		printf("Não existe");
            break;
        case 3: // neighbors
        	printf("vert1\n");
        	scanf("%d ", &x);
        	free(neighbors(G, x));
            break;
        case 4: // removeEdge
        	printf("vert1, vert2\n");
        	scanf("%d %d ", &x, &y);
        	removeEdge(G, x, y);
        	break;
        case 5: // printInfo
        	printInfo(G);
        	break;
        case 6: // deleteGraph
        	deleteGraph(&G);
        	break;
        case 7: // maxNeighbors
        	printf("maxNeighbor: %d\n", maxNeighbors(G));
        	break;
        case 8: // adjacencyMatrix
        	adjacencyMatrix(G);
        	break;
        default:
            printf("unrecognized option!\n");
    }
    
    goto loop;
    
    fim_loop:

    return 0;
}
